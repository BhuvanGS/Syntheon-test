let display = document.getElementById('display');let currentNumber = '';let previousNumber = '';let operator = '';document.addEventListener('keydown', (e) => { if (e.key === '0' || e.key === '1' || e.key === '2' || e.key === '3' || e.key === '4' || e.key === '5' || e.key === '6' || e.key === '7' || e.key === '8' || e.key === '9' || e.key === '+' || e.key === '-' || e.key === '*' || e.key === '/' || e.key === '.') { handleInput(e.key); } else if (e.key === '=' || e.key === 'Enter') { calculateResult(); } });document.querySelectorAll('.button').forEach((button) => { button.addEventListener('click', () => { if (button.id === 'clear') { clearDisplay(); } else if (button.id === 'backspace') { backspace(); } else if (button.id === 'equals') { calculateResult(); } else { handleInput(button.textContent); } }); });function handleInput(input) { if (input === '+' || input === '-' || input === '*' || input === '/') { operator = input; previousNumber = currentNumber; currentNumber = ''; display.value = ''; } else if (input === '.') { if (!currentNumber.includes('.')) { currentNumber += '.'; display.value = currentNumber; } } else { currentNumber += input; display.value = currentNumber; } }function calculateResult() { if (operator === '+') { display.value = parseFloat(previousNumber) + parseFloat(currentNumber); } else if (operator === '-') { display.value = parseFloat(previousNumber) - parseFloat(currentNumber); } else if (operator === '*') { display.value = parseFloat(previousNumber) * parseFloat(currentNumber); } else if (operator === '/') { if (currentNumber !== '0') { display.value = parseFloat(previousNumber) / parseFloat(currentNumber); } else { display.value = 'Error: Division by zero'; } } currentNumber = display.value; previousNumber = ''; operator = ''; }function clearDisplay() { display.value = ''; currentNumber = ''; previousNumber = ''; operator = ''; }function backspace() { currentNumber = currentNumber.slice(0, -1); display.value = currentNumber; }